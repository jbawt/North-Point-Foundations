# Optional: Netlify (and similar) can use this for cache headers on static deploys.
# `netlify.toml` in the repo root is the primary place to configure Netlify.

/assets/*
  Cache-Control: public, max-age=31536000, immutable
